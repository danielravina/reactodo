var React = require("react")

var InputBox = React.createClass({
  render: function() {
    return (
      <div className="inputbox">
        input box here?
      </div>
    );
  }
});

module.exports = InputBox;